$("body").animate({"scroll-top": "0px"}, function(){
  html = "<%= j(render 'form') %>"
  $("#new_feed_url_div").remove();
  overlay()
  $(".navbar").append(html)
  $("#new_feed_url_div").center();
})
